= Lab5

== Q: 下图为一种 74LS138 功能实现的电路图。请使用两个 74LS138 模块和尽可能简单的电路逻辑组成一个 4-16 译码器，给出电路图并进行解释。

#image("D_74LS138.png", width: 80%)

== A: 如图所示：

#image("4to16decoder.png", width: 80%)

接线思路：
用两片74LS138的A、B、C都接系统的低三位地址A0、A1、A2，
最高位地址A3用于控制两片的使能端，保证两片74LS138只有一片在工作。

第一片（输出0~7）：
G1固定接高，G2B固定接低，G2A接A3。
当A3=0时，G2A=0，全部使能条件满足(1,0,0)，芯片工作；
当A3=1时，G2A=1，使能无效，输出全高。

第二片（输出8~15）：
G2A、G2B固定接低，G1接A3。
当A3=1时，G1=1，使能条件满足(1,0,0)，芯片工作；
当A3=0时，G1=0，使能无效，输出全高。

两片互斥使能，无需额外逻辑门，A3直接作为选择器，即构成4-16译码器。